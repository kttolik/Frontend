const numReflection = require('../task027');

describe('Calculate the sum of digits of a given number', () => {
    test('numReflection', () => {
        expect(numReflection(123)).toBe('321');
    })
})
